# Brief: retire a stateful cache

Decommission a stateful cache component without losing the required export.
The delivery bundle must make the operator choose the retention/export location,
verify dependents no longer use the cache before deletion, record residual risk,
and require explicit destructive authorization.  Do not treat removal from
inventory as decommission completion.
