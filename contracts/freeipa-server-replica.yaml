schemaVersion: 1
id: freeipa-server-replica
role: freeipa-server-replica
specs:
  - path: docs/verification/freeipa-server-replica.md
    rows: {all: true}
playbooks:
  apply: playbooks/apply/freeipa-server-replica-apply.yml
regressionTests:
  - internal/spec/freeipa_server_replica_regression_test.go
  - cmd/pilot/cmd/tag_coverage_test.go
dependencies:
  - {component: freeipa-server, required: true, relation: providerEndpoint}
bindings:
  - input: freeipa_server_ip
    requiredWhenDependencySelected: true
    sourceSelection: exactlyOne
    from: {component: freeipa-server, endpoint: https}
hostCardinality: one-or-more
resources: {minCPU: 2, minRAMMiB: 4096, minDiskGiB: 20}
groupVars:
  - {name: freeipa_server_ip, type: string, required: true, secret: false}
  - {name: ipa_admin_password, type: string, required: true, secret: true}
stagePolicy: {variable: stage, default: sandbox}
evidenceRequirement: {targetTest: topology, idempotency: required}
traceability:
  mode: mapped
  rows:
    "docs/verification/freeipa-server-replica.md#C1": {tags: [R1, R2], reason: client enrollment followed by replica promotion}
    "docs/verification/freeipa-server-replica.md#C2": {tags: [R2], reason: replica service health}
    "docs/verification/freeipa-server-replica.md#C3": {tags: [R1], reason: replica hostname setup}
    "docs/verification/freeipa-server-replica.md#C4": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C5": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C6": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C7": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C8": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C9": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C10": {tags: [R2], reason: replica install listener}
    "docs/verification/freeipa-server-replica.md#C11": {tags: [R2], reason: replicated directory suffix}
    "docs/verification/freeipa-server-replica.md#C12": {tags: [R2], reason: replica CA setup}
    "docs/verification/freeipa-server-replica.md#C13": {tags: [freeipa-replica-verify], reason: replicated sudo subtree verification}
    "docs/verification/freeipa-server-replica.md#C14": {tags: [freeipa-replica-verify], reason: topology verification}
    "docs/verification/freeipa-server-replica.md#C15": {tags: [freeipa-replica-verify], reason: topology verification}
verification: {autoDeploy: false}
site: {include: false, order: 0, vars: {}, tags: [], optIn: true}
