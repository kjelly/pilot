# Detection Engine actionable-notification evidence — 2026-09-17

## Candidate identity

| Field | Value |
|---|---|
| Tested commit | `30d6b92c4a69a9af2e4f7b219d51e740c0a9e3f4` |
| Tested tree | `ce03c8b205d4d68e2e5b3c533a1bb84b8ad8ce93` |
| Checkout | clean detached worktree `/tmp/pilot-detection-30d6b92` |
| Binary SHA-256 | `3521409da9937dfcbb4ac122a5e36075f550d8267c885dd750a9a5e0d4846186` |
| Raw verify artifact | `.verification/detection-engine-20260917-063632.{md,ndjson}` |

Execution-affecting hashes:

| File | SHA-256 |
|---|---|
| `cmd/pilot-detection-engine/main.go` | `f839f77871313e5677ec29a9bd9412751c6f88930bc5e5e6fe808176f7f865a6` |
| `internal/detection/engine.go` | `1a2eb5ad493a4b11437bc3eedd25137c73a58be5a03f54e8ee84634b3265ea0d` |
| `internal/detection/episode.go` | `075851368b32104afe744c89da60fe0f8b9c7c2aee390086359a5b603c5000ce` |
| `internal/detection/featureprofile.go` | `1de8c9c958739cdbc29e673988d5fe8297eb86cc0e326523c780c68e5c5bd421` |
| `monitoring/detection/feature-profiles/linux-host-v1.yaml` | `3db7ec70b7da26da62c76abd821ba4320e088bb955ae1bc942f388457b2492fb` |
| `playbooks/apply/detection-engine-apply.yml` | `8b6197bfbef8815627b2b1904c8a472fb83eddd6d355e773bf7290623a6197b7` |
| `docs/verification/detection-engine.md` | `2a37ab41895a64f4ad93b6e7145906fe04de7b291a72eec31fa7b9b83f6be0d6` |

## Target and inventory facts

The existing fake-protocol topology was provisioned from
`tmp/detection-engine-fake-topology.example.yaml`. Rendered inventory facts:

| Group | Host | IP |
|---|---|---|
| `detection-engine`, `host-monitoring` | `detection-engine-fixture` | `192.168.122.10` |
| `detection-fixture-source` | `detection-fixture-source` | `192.168.122.9` |
| `detection-fixture-sink` | `detection-fixture-sink` | `192.168.122.8` |
| `detection-fixture-logs` | `detection-fixture-logs` | `192.168.122.6` |
| `detection-fixture-provider` | `detection-fixture-provider` | `192.168.122.11` |

The apply used the fake Thanos source, fake Alertmanager sink, and fake Loki
known-critical-pattern input. `host-monitoring` was applied first on the
Detection node to provide its required textfile collector.

## Automated results

| Gate | Result |
|---|---|
| Detection spec lint | PASS — 12 rows, 0 findings |
| Focused Go regression | PASS — 436 tests in `internal/detection`, `cmd/pilot-detection-engine`, and `internal/spec` |
| Full Go suite | 3699 PASS, 5 unrelated pre-existing `cmd/pilot/cmd` TUI timing failures, 1 skipped; all five failures reproduce alone and touch no changed file |
| L1 syntax | PASS |
| L3 fresh-target `--check --diff` | PASS |
| L4 apply | PASS — `ok=61 changed=11 failed=0 unreachable=0` |
| L5 verify | PASS — C1–C12, `pass=12 fail=0 skip=0` |
| L6 idempotency | PASS — `ok=59 changed=0 failed=0 unreachable=0` |

## Runtime notification scenario

The fake Loki hard trigger created critical episode
`01M2Q1AGZMYJ327DGYGEQ4B00X` using profile version 4. The fake Alertmanager
received a valid JSON alert object with:

- stable `pilot_subject`, `signal_id`, `action_required=true`, and
  `notification_channel=teams` labels;
- `reason`, `recommended_action`, `runbook_url`, and `duration_seconds`
  annotations;
- initial `startsAt=2026-09-17T06:36:30Z` and
  `endsAt=2026-09-17T06:39:30Z`.

After 60 seconds, the runtime delivered `heartbeat_refresh` for the same
signal and exact labels, with `duration_seconds=60` and
`endsAt=2026-09-17T06:40:30Z`. Restarting
`pilot-detection-engine.service` preserved one active episode with the same
signal ID (`signals.active=1`), proving startup hydration rather than a new
episode.

The fake Loki input was then changed from OOM to a benign line. The episode
entered `recovering` without creating an outbox notification. After eight
quiet cycles it resolved at `2026-09-17T06:41:30Z`. The resolve outbox row was
`delivered`, the fake sink accepted it, and its labels were byte-for-byte
equal to the initial fire labels (`same_labels=True`); this directly closes
the former HTTP 422 defect. Five total sink deliveries were observed: one
fire, three 60-second heartbeats, and one resolve.

## Setup deviations retained as evidence

- The fixture playbook's old `--sandbox` example now selects a Docker
  sandbox without `ssh`; that attempt failed before reaching any VM. The
  fixture was rerun through `pilot vm-target run` with its existing lint debt
  explicitly skipped.
- The Ubuntu base image had a stale apt index and the first
  `host-monitoring` prerequisite run hit a package 404. A sanctioned
  `pilot vm-target exec -- sudo apt-get update` refreshed the disposable VM;
  the retry passed. Neither issue was caused by the candidate runtime.

