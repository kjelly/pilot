# Captive SSH transport — Phase 2: broker, known-hosts, gateway API (2026-09-23)

Spec: `docs/superpowers/specs/2026-09-23-pilot-access-gateway-captive-ssh-transport-spec.md` §7–§11, §12.1, §16 Phase 2. This phase adds Go code only; nothing is deployed until Phase 3.

| Item | Value |
|---|---|
| Tested candidate | `0e865d2` (tree `37c79894eb73e673ffd91c904261f69abdf10c54`), clean detached worktree |
| Toolchain | go1.26.4; golangci-lint v2.12.1 built with go1.26.4 (the version CI pins) |
| Verdict | **PASS** |

## Gate results at the candidate

- `go build ./...`: OK. `go vet ./cmd/... ./internal/...`: OK.
- `go test -race`, all ok: `cmd/pilot/cmd` (508.8 s), `cmd/pilot-access-gateway`, `internal/gatewayapi`, `internal/sessionaudit`, `internal/freeipaaccess`, `internal/spec`, `internal/contract`.
- `golangci-lint run ./...`: `0 issues`.
- The full-repo `go build ./... && go vet ./... && go test -race ./...` also passed (rc=0) on the pre-commit working tree. The only later change was fixing errcheck in one test file.

## Row → test mapping (all passing)

| Row | Test(s) |
|---|---|
| AG45 | `TestParsePortalSSHOriginalCommand_ValidTransport`, `_ValidKnownHosts` |
| AG46 | `TestParsePortalSSHOriginalCommand_RejectsTransport` (22 bad targets × both verbs, plus near-miss verbs) |
| AG47 | `TestRunPortalSession_TTYPolicy` |
| AG48 | `internal/gatewayapi` `TestConnectAuthorize_TransportGate` (disabled → no FreeIPA call, direct/nested ready, not ready, lookup failure, unauthorized) |
| AG49, AG51, AG56 | `TestRunPortalTransport_DeniedPathsNeverResolveOrDial` (7 deny paths: zero resolves, zero dials, empty stdout, stable message, denied audit reason) |
| AG50 | `TestRunPortalTransport_FreshAuthorizeEveryCall` |
| AG52 | `TestRunPortalTransport_ResolveOnceDialExactIP22` (+ at-most-3, DNS failure, special-only, local-address failure) |
| AG53 | `TestFilterTransportAddrs` |
| AG54 | `TestBridgeTransport_ByteForByte` (16 MiB random with NUL/0xff/CR/LF) |
| AG55 | `TestBridgeTransport_HalfClose` (final bytes after stdin EOF; bounded exit with a never-closing stdin; cancellation closes the socket) |
| AG57 | `TestRunPortalTransport_AuditEvents` (requested → connected → closed with IP/bytes/duration; payload never in the audit stream) |
| AG58 | `TestParseHostSSHPublicKeys` (real Phase 0 `host_show` capture), `TestKnownHostsLine`, `TestRunPortalKnownHosts`, `internal/gatewayapi` `TestTransportHostKeys` |
| AG59 | `cmd/pilot-access-gateway` `TestLoadConfigTransport` |

The pre-existing AG35–AG40 tests (`TestParsePortalSSHOriginalCommand_ValidConnect`/`_Rejects`, `TestRunPortalOneShotConnect_*`) still pass against the new typed parser.
