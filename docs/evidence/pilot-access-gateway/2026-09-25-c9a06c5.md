# Captive SSH transport rev 5 proposal — D8 also refuses host keys, candidate `c9a06c5` (2026-09-25)

Design: `docs/superpowers/specs/2026-09-23-pilot-access-gateway-captive-ssh-transport-spec.md`
rev 5, a proposal for the transport spec owner.

## Tested revision

| Item | Value |
|---|---|
| Candidate | **commit `c9a06c5`, tree `8e7dc3300d0ce4e86e74be71dc8e60da51879edd`**, on `main` `548f35b` |
| Checkout | a clean worktree of that commit (`git status` empty before and after); `pilot` and `pilot-access-gateway` built there |
| Topology | `docs/topologies/pilot-access-transport-topology.yaml` with the node names changed from `tx-` to `kh-` in a per-run copy (VMs named `tx-*` belonged to another run): `kh-ipa` 192.168.122.5 (AlmaLinux 9), `kh-gw` .9 (gateway `gpu-01`, scope `gpu`), `kh-target` .8, `kh-ws` .3 (Ubuntu 24.04), fresh from `topology up`. Torn down afterwards |
| Verdict | **PASS** |

## Behaviour before and after

| Recording mode | rev 4 (`main`) | rev 5 (`c9a06c5`) |
|---|---|---|
| `""` / `metadata` | transport and host keys served | unchanged |
| `terminal_output`, `terminal_io`, unknown | transport refused by the `pilot-transport-v1` command; `/v1/transport/host-keys` still returned the keys | the gateway's transport gate refuses both: `transport_deny_reason: recording_incompatible`; host keys `allowed:false`, `[]`; `pilot-known-hosts-v1` prints `access denied` |

The transport message and audit result are unchanged
(`pilot-transport: transport disabled by recording policy`,
`recording_incompatible`). The command keeps its own check for gateway
daemons from before this change.

## Topology test on never-applied VMs

The first attempt failed at L4 in `gateway-scope-apply.yml`:
`ipa hostgroup-add-member pilot-target-gpu --hosts=tx-target` rc 1. `main`'s
`playbooks/test/pilot-access-transport-e2e.yml` names the target `tx-target`
literally, so a renamed copy of the topology cannot use it. The run rolled
every node back. The second run added
`-e '{"gateway_scope_hosts": ["kh-target.ipa.pilot.internal"]}'`; the
playbook's defaults yield to extra vars.

| Step | Result |
|---|---|
| L1 syntax | passed |
| L3 `--check --diff` | passed, `failed=0` on all four |
| L4 apply | `failed=0` (`kh-gw changed=50`, `kh-target 26`, `kh-ipa 22`, `kh-ws 3`) |
| L5 verify | gateway spec **16/16**, target policy spec **5/5** |
| L6 idempotency | `changed=0` on all four |

## E2E — `scripts/pilot-access-gateway-transport-e2e.sh`

| Gateway state | Phase | Result |
|---|---|---|
| transport on, recording `metadata` | strict | **20/20**; AG67: known-hosts served the target's keys, inner OpenSSH connected with `StrictHostKeyChecking yes` |
| transport on, recording `terminal_output` | recording | **3/3**: `AG69-transport-denied-by-recording` `pilot-transport: transport disabled by recording policy` rc=1; **`AG69-known-hosts-denied-by-recording` `pilot-known-hosts: access denied` rc=1** (new); `AG69-pilot-connect-still-records`: `recording_started` 1, the local FileSink file (`transportuser`, 600, 5 events) held the output marker |
| back to `metadata` (`recording_allow_downgrade=true`) | strict | **20/20**: keys served again |
| transport `false` | disabled | **4/4** |

Gateway audit during the recording phase (`journalctl -t pilot-access-gateway`):
`gateway_transport_denied` result `recording_incompatible`, recording mode
`terminal_output`; then `gateway_known_hosts_denied` result `denied`. The
strict phase before it logged `gateway_known_hosts_served ok`.

Each gateway state change was an apply of
`playbooks/apply/pilot-access-gateway-apply.yml` with rc 0, `changed=2`
(config, restart).

## Tests and checks on `c9a06c5`

| Check | Result |
|---|---|
| `go build ./...`, `go vet ./...`, golangci-lint | pass, 0 issues |
| `go test -race -count=1 ./...` | rc=0, 52 packages |
| New test cases | `TestConnectAuthorize_TransportGate`: metadata, `terminal_output`, `terminal_io`, an unknown mode, disabled-and-terminal; `TestTransportHostKeys`: `terminal_output`; `TestRunPortalTransport_OlderGatewayRecordingStillDenied` |
| Each fails when its change is removed | without the server check: the gate cases for `terminal_output`, `terminal_io`, the unknown mode, and the host-key case; without the client check: the older-daemon test; without the reason mapping: the three recording cases of `TestRunPortalTransport_DeniedPathsNeverResolveOrDial` |

## Not covered

- The remote-dev and not-ready phases and the lockout suite were not rerun.
  The change adds a step before the readiness lookup, and those paths carry
  the `metadata` mode.
- `terminal_io` and unknown modes are covered by unit tests only.
