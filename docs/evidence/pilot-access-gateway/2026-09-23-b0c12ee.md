# Captive SSH transport — Phase 1: recording policy owned by group vars (2026-09-23)

Spec: `docs/superpowers/specs/2026-09-23-pilot-access-gateway-captive-ssh-transport-spec.md` §12.2, §16 Phase 1.

| Item | Value |
|---|---|
| Tested candidate | `b0c12ee` (tree `54701dcfae8ea2414a2f53e9994bbccab1a0ce25`), built and run from a clean detached worktree |
| Topology | `docs/topologies/pilot-access-transport-topology.yaml` (4 nodes, `tx-*`), already provisioned by the Phase 0 run |
| Secrets | `-e @<vault>` supplying `ipa_admin_password` only |
| Go tests at candidate | `internal/spec`, `internal/contract`, `internal/inventory` (990 passed); `cmd/pilot/cmd` bookkeeping subset `TagAlignment\|Contract\|Spec\|Catalog\|Site\|Apt` (105 passed) |
| Verdict | **PASS** |

## Topology test (AG01–AG41 host rows)

```text
pilot vm-target topology test --topology docs/topologies/pilot-access-transport-topology.yaml \
    --playbook playbooks/test/pilot-access-transport-e2e.yml \
    --verify docs/verification/pilot-access-gateway.md=pilot-access-gateway -- -e @<vault>
```

| Step | Result |
|---|---|
| L1 syntax | passed |
| L3 `--check --diff` on current state | passed (`tx-gw changed=5`, all others `changed=0`, `failed=0`) |
| Cluster snapshot | 4 nodes, tag `pre-test-1790153072` |
| L4 apply | `tx-gw ok=197 changed=8`, `tx-ipa ok=48 changed=2`, `tx-target ok=128 changed=0`, `tx-ws ok=5 changed=0`; `failed=0` |
| L5 `pilot verify` | **PASS 13/13** (AG01–AG04, AG06, AG09, AG12, AG19, AG30, AG32–AG34, AG41) |
| L6 idempotency | `changed=0` on all four hosts |

Before this candidate, the same spec verified as `pass=12 fail=7`: the environment-specific AG01 plus the never-executable AG35–AG40. This candidate moved AG35–AG40 out of the checklist and aligned the wrapper's gateway identity with the spec's reference values (`gpu-01`/`gpu`).

## AG60 — recording policy cannot be silently lowered

All runs used `playbooks/apply/pilot-access-gateway-apply.yml -l tx-gw` from the candidate worktree.

| # | Extra vars | rc | Result | `/etc/pilot/access-gateway.yaml` afterwards |
|---|---|---|---|---|
| 1 | `pilot_access_gateway_recording_mode=terminal_output` | 0 | upgrade applied (`changed=2`: config + service restart) | sha256 `3823fb8b…`, `mode: "terminal_output"` |
| 2 | (none → metadata) | 2 | `Refusing to lower this gateway's session-recording policy: installed mode=terminal_output session_store_url=(none), requested mode=metadata …` | unchanged, sha256 `3823fb8b…` |
| 3 | `pilot_access_gateway_recording_allow_downgrade=true` | 0 | downgrade applied | sha256 `26fd1314…`, `mode: "metadata"` |
| 4 | `…_recording_mode=terminal_io` | 0 | upgrade applied | `mode: "terminal_io"` |
| 5 | (none) with `--check --diff` | 2 | same refusal — the guard works in check mode | unchanged |
| 6 | `…_recording_mode=terminal_output` | 2 | refused: `installed mode=terminal_io …, requested mode=terminal_output` (a partial downgrade is still a downgrade) | unchanged |
| 7 | `…_recording_allow_downgrade=true` | 0 | restored to metadata for the next phases | `mode: "metadata"` |

## Deviations / notes

- The SSSD group-cache issue recorded in the Phase 0 evidence (`IsMemberOfGroup` reading a stale `getent group` member list) is still open and awaiting a separate decision. It did not affect this phase: nothing here calls the user-facing API as a portal user.
- The snapshot tag `pre-test-1790153072` remains on the four VMs; it is disposable.
