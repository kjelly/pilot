# Captive SSH transport — Phase 3: gateway deployment integration (2026-09-23)

Spec: `docs/superpowers/specs/2026-09-23-pilot-access-gateway-captive-ssh-transport-spec.md` §12.3–§12.6, §15.4, §16 Phase 3.

| Item | Value |
|---|---|
| Tested candidate | `a568caa` (tree `689b2bcfd7a6d844e07dd4977096d5d6655b74a6`), built and run from a clean detached worktree; the gateway ran the Phase 2 Go code (`0e865d2`) plus this candidate's playbook |
| Topology | `docs/topologies/pilot-access-transport-topology.yaml` (`tx-ipa`, `tx-gw`, `tx-target`, `tx-ws`) |
| Portal user | `transportuser`, authenticating with the disposable fixture key (`PORTAL_KEY`) |
| Verdict | **PASS** |

## Topology test (host rows AG01–AG44)

| Step | Result |
|---|---|
| L1 syntax | passed |
| L3 `--check --diff` on current state | passed (`tx-gw changed=7`, `failed=0` on all hosts) |
| Cluster snapshot | tag `pre-test-1790155041` |
| L4 apply | `tx-gw ok=198 changed=7`; all hosts `failed=0` |
| L5 `pilot verify` | **PASS 16/16** (AG01–AG04, AG06, AG09, AG12, AG19, AG30, AG32–AG34, AG41–AG44) |
| L6 idempotency | `changed=0` on all four hosts |

## Lockout suite — `scripts/pilot-access-gateway-lockout-test.sh`

`SUMMARY: 28 passed, 0 failed`. One probe (`lockout-transport-no-child`) was skipped because this phase has no transport-ready target; it is run in Phase 5.

| Group | Probes | Real result (excerpt) |
|---|---|---|
| Pre-existing | forced-PTY injection, `RemoteCommand`, `-L`, SFTP subsystem, `sshd -T -C` (ForceCommand/DisableForwarding/PermitUserRC/X11/TCP/agent/tunnel), 8 `pilot-connect` grammar cases | all pass; `-L` → `channel 2: open failed: administratively prohibited` |
| New directives | `allowstreamlocalforwarding no` (Match), global `permituserenvironment no` | pass |
| New §15.4 probes | no-PTY arbitrary command | `rc=1`, `pilot-session: unrecognized command` |
| | `ssh -tt … pilot-transport-v1` / `pilot-known-hosts-v1` | `rc=1`, `a TTY is not allowed …` |
| | `scp` (SFTP protocol) / `scp -O` | `rc=255` / `rc=1`, nothing written to the gateway |
| | `-R` | `Error: remote port forwarding failed for listen port 18023` |
| | `-D` + SOCKS connect | `channel 1: open failed: administratively prohibited` |

Probe 1 now passes on the dispatcher's explicit rejection. Since Phase 5 (2026-09-18) an unrecognized command no longer falls through to the TUI, so the old "`pilot-session → pilot portal` in the tree" expectation was obsolete.

## Default-off transport and legacy paths

| Probe (from the controller, `transportuser`) | Result |
|---|---|
| `ssh -T gw -- pilot-transport-v1 tx-target…` | `rc=1`, `pilot-transport: transport not enabled for this target` (config renders `enabled: false`) |
| `ssh -T gw -- pilot-known-hosts-v1 tx-target…` | `rc=1`, `pilot-known-hosts: access denied` |
| `ssh -T gw` (portal without TTY) | `rc=1`, `pilot-session: a TTY is required for this session` — the Go-side replacement for the wrapper's removed shell TTY test |
| `ssh -tt gw` (portal with TTY, pty 120×40) | TUI rendered: `Pilot Portal / Gateway gpu-01 / Scope gpu / User transportuser / > My Hosts / My Identity / Refresh / Logout` |
| `ssh -tt gw -- pilot-connect <uuid> tx-target…` | reached `Kerberos password for transportuser:`, i.e. the legacy one-shot path authorized and moved on to its unchanged credential step |

## Deviations / follow-ups

- Every `pilot portal-session` error was printed twice, once by cobra (`Error: …`) and once by `cmd/pilot/main.go`. Because this stderr is shown straight in the user's terminal on the transport path, `portal-session` now sets `SilenceErrors` so the stable message appears exactly once. That fix is a separate commit that follows this evidence; this candidate itself still printed the message twice.
- The SSSD group-cache issue from the Phase 0 evidence (`IsMemberOfGroup`) is still open, awaiting a decision; it did not occur during this run.
