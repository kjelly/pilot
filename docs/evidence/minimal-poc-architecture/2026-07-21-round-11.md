# Minimal PoC architecture — round 11 evidence summary

> Evidence class: legacy sanitized summary
> Verified at: 2026-07-21T17:30Z
> Original record commit: `04809b551599bc42a3fb833e97c6f2cf04d0c6d8`
> Current runbook: [`minimal-poc-architecture.md`](../../runbooks/minimal-poc-architecture.md)

This run predates `AGENTS.md` v1.16 and therefore did not record an immutable tested commit/tree,
execution-affecting file hashes, target image digests, or a durable raw-artifact checksum. The
results below are retained from the real round-11 transcript without claiming those missing fields.

## Target facts

- Fresh disposable VMs: `freeipa-server` (AlmaLinux 9), `nexus` (Ubuntu 24.04), and `client-vm`
  (Ubuntu 24.04).
- Inventory and variables were produced through `pilot edit` and `pilot inventory generate`.
- Complete preflight reported `failed=0` for all three hosts.
- Site deployment used `pilot deploy -i <generated-inventory> --timeout 90m`.
- All inventory-derived variable prompts were accepted; the manual extra-`-e` field remained empty.
- FreeIPA identity was applied separately through `pilot reconcile` using the roster vars file.

## Real results

| Check | Result |
|---|---|
| Site apply — `client-vm` | `ok=83 changed=41 failed=0` |
| Site apply — `freeipa-server` | `ok=35 changed=10 failed=0` |
| Site apply — `nexus` | `ok=154 changed=74 failed=0` |
| Site apply — `localhost` | `ok=1 changed=0 failed=0` |
| Identity apply | `freeipa-server ok=31 changed=12 failed=0` |
| FreeIPA service | Active |
| Grafana health | HTTP 200 |
| Prometheus readiness | HTTP 200 |
| Loki readiness | HTTP 200 |
| Thanos query | `up{job="prometheus",site="site-nexus"}=1` |
| Restic | Timers active; shared repository contained fresh snapshots |
| Wazuh FIM | Real-time `/etc` file-add alert with `whodata` received |
| Live sudo allow | Authorized `systemctl` command succeeded |
| Live sudo deny | Reading `/etc/shadow` was denied |
| Reconciler remove/restore | Membership removal and restore plus command drift correction passed |
| Final reconcile rerun | `changed=2 failed=0`; residual changes were reported, not normalized to zero |

## Evidence limitations

- The tested pilot commit/tree was not recorded separately from the later documentation commit.
- Raw recordings were disposable and have no durable archive ID or checksum in the legacy record.
- The retained transcript contained ephemeral lab addresses and internal identities; this summary
  omits those values.
- Site-wide second-apply idempotency was not separately preserved in the compact round-11 excerpt;
  only the identity reconciler's final rerun count is retained.

The next formal run must create a frozen candidate first and satisfy
[`docs/actual-run-evidence.md`](../../actual-run-evidence.md).
