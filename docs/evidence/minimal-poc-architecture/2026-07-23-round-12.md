# Minimal PoC architecture — round 12 / CAND-23 evidence summary

> Evidence class: immutable-candidate clean-room revalidation
> Verified at: 2026-07-23T03:25+08:00
> Current runbook: [`minimal-poc-architecture.md`](../../runbooks/minimal-poc-architecture.md)

## Immutable provenance

- Candidate commit: `4034ccd6cc7290a3dd625ea2e6e15d825c9373a3`.
- Candidate tree: `d72c9f9c21bc531d0d3127e3b03c70d20594f6af`.
- Candidate binary SHA-256: `862f9ad144bb04a162c71187590d2ba099f7ca86da9313385ca910a73168b96e`.
- AlmaLinux 9 golden image SHA-256: `b780998adca7c69ff6dbd41a05b51769ef6ab6708a7defbd286195186fbcc8ac`.
- Ubuntu 24.04 golden image SHA-256: `bfab799e2c50a83f77c607e50290646c07628a67ed73fe90c28658a28b0a5394`.
- Formal execution used the isolated checkout
  `/tmp/pilot-minimal-poc-r12-candidate-v23-formal-clean` and a newly authored workspace beneath
  it. No execution-affecting candidate file changed after the commit was frozen.

The provenance recording is
`.verification/minimal-poc-update/2026-07-22-round-12/formal-integrity-cand23/provenance.cast`
(SHA-256 `3ddb727573ae31f24110b3cc8e608052f972dea2282e664ed0e45dd873b46e56`).

## Clean-room target and inventory facts

- Fresh disposable targets: `freeipa-server` (AlmaLinux 9; 2 vCPU, 4096 MiB, 30 GiB), `nexus`
  (Ubuntu 24.04; 6 vCPU, 12288 MiB, 80 GiB), and `client-vm` (Ubuntu 24.04; 2 vCPU,
  2048 MiB, 20 GiB).
- Inventory was authored only through `pilot edit` and `pilot inventory generate`; the canonical
  identity/NFS roster was the documented direct-authoring exception. Vault and roster modes were
  both `0600`.
- Nexus was present in `freeipa-client`, `freeipa-nfs-server`, `docker`, `wazuh-manager`,
  `wazuh-fim`, `audit-log-forwarding`, `seaweedfs-s3`, `restic-backup`, `prometheus`,
  `thanos-query`, `alertmanager`, and `dashboard`.
- `client-vm` was present in `freeipa-client`, `freeipa-nfs-client`, `docker`, `wazuh-fim`,
  `audit-log-forwarding`, and `restic-backup`.
- Ephemeral addresses are intentionally omitted. The raw inventory graph is retained in the
  workspace evidence directory.

## Real apply and idempotency results

| Operation | Real result |
|---|---|
| FreeIPA server bootstrap | `ok=29 changed=10 failed=0` |
| Nexus + client-vm FreeIPA enrollment | each `ok=25 changed=13 failed=0` |
| Canonical identity initial reconcile | `freeipa-server ok=65 changed=21 failed=0` |
| Fresh full-site apply | `client-vm ok=101 changed=31`; `freeipa-server ok=72 changed=25`; `nexus ok=220 changed=87`; all `failed=0` |
| Identical second full-site apply | `client-vm ok=95 changed=0`; `freeipa-server ok=67 changed=0`; `nexus ok=207 changed=0`; all `failed=0` |
| Canonical identity final no-op reconcile | `freeipa-server ok=64 changed=0 failed=0` |

CAND-23's create-once NFS rollback state was exercised from an initially absent managed exports
fragment. On the identical second site apply both snapshot tasks skipped and Nexus remained
`changed=0`.

## Signed S3 and functional verification

Full-site deployment received restic/Thanos credentials from the generated vault. It automatically
rendered `/etc/seaweedfs/s3.json`, started SeaweedFS with the signed config, created the restic and
Thanos buckets, and completed real backups.

| Contract/probe | Result |
|---|---|
| SeaweedFS signed config mode | `0600` |
| Live SeaweedFS container command | contains `-s3.config=/etc/seaweedfs/s3.json` |
| restic-backup | `30/30` pass (three hosts; repository integrity included) |
| FreeIPA server / clients / identity | `18/18`, `20/20`, `18/18` pass |
| FreeIPA NFS server / client | `8/8`, `6/6` pass |
| Prometheus / Thanos Query / Alertmanager | `12/12`, `10/10`, `7/7` pass |
| Dashboard / Docker | `14/14`, `16/16` pass |
| Wazuh FIM / audit-log-forwarding | `27/27`, `57/57` pass |
| Co-located Promtail path | effective glob points at `single-node_wazuh_logs/_data/**/*.log` |
| Co-located Loki stream | `pilot-siem` query probe passed |

The first restic verification used a 60-second per-row timeout and reported `29/30` because the
FreeIPA host's full C6 integrity check timed out. The unchanged spec was rerun with a 300-second
timeout and passed `30/30`; both recordings are retained.

## Documented applicability exceptions

- `seaweedfs-s3.md` remains an anonymous-mode contract. In the required signed mode its unsigned
  C6–C8 requests are rejected, so the expected result is `5 pass / 3 fail`. Signed config/runtime
  probes and authenticated restic/Thanos checks passed; authentication was not weakened to satisfy
  the legacy rows.
- This three-node topology has no independent `log-server`. `wazuh-manager.md` therefore reports
  the documented optional C11 failure (`10/11`), and `log-shipping.md` reports C3/C6 as inapplicable
  (`5/7`). The co-located Wazuh-volume path and actual Loki stream probes passed.

## Raw evidence integrity

Raw recordings and generated verify reports are retained under
`.verification/minimal-poc-update/2026-07-22-round-12/` in the `candidate-cand23-gates`,
`formal-vm-cand23`, `formal-workspace-cand23`, `formal-deploy-cand23`,
`formal-reconcile-cand23`, `formal-site-cand23`, `formal-fixtures-cand23`,
`formal-verify-cand23`, and `formal-integrity-cand23` directories.

The critical evidence audit checked 51 recordings: 51 valid, 0 invalid, every secret scan complete
with zero findings. Its recording is
`formal-integrity-cand23/trec-verify-critical.cast` (SHA-256
`b87f799a5e6b097b5b672cabc142ca85fc918673c7b74eb87f5277cd117f06d4`). Five separate failed
attempt recordings were deliberately retained (wrong deploy cwd, pre-backfill group-vars edit,
the short restic timeout, and two obsolete probe invocations); they are not part of the successful
critical set and are described rather than deleted.

Secret values and ephemeral lab addresses are omitted from this publication-safe summary.
