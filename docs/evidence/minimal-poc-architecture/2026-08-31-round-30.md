# Round 30 evidence — 2026-08-31

> Focus: full clean-room revalidation of the entire §0.5–§4.5 scope from a genuinely fresh 3-node
> topology, plus a complete idempotency-rerun sweep (both day-2 reconcilers together, and each of
> the three single-component deploys individually). No new feature was under test — this round's
> value was finding, and fixing with explicit authorization, 2 real regressions introduced by very
> recent unrelated work that had landed on `HEAD` roughly an hour before this round started.

## Candidate

`HEAD` at round start: commit `0bb39cc`. This round's own 2 fixes remain uncommitted at the time of
this write-up:

1. `playbooks/apply/tasks/freeipa-client-host-dns.yml` — added `and not ansible_check_mode` to the
   "Gate: authoritative DNS queries must return a usable response" task's `when:`.
2. `playbooks/apply/freeipa-nfs-server-apply.yml` line 38 — the roster schema-version gate changed
   from `[1, 2]` to `[1, 2, 3]`.

Each fix has a dedicated regression test added under `internal/spec/`. No other Ansible playbook,
Go source, or product implementation file was modified this round.

## Process — two real regressions found and fixed, then a clean full-scope pass

Both regressions were introduced by `0bb39cc` itself (this round's own starting `HEAD`, landed
roughly an hour before this round began) and were not present in round 29's candidate.

| # | Symptom | Cause | Outcome |
|---|---|---|---|
| 1 | Site-wide `--check --diff` preview fatal on `client-vm`/`nexus`: "Cannot verify the authoritative DNS state ... Refusing DNS registration ... (spec.md §8.2/§8.3, fail-closed)" | A new fail-closed DNS gate had no check-mode exemption. A `--check` preview correctly skips `ipa-server-install` (a real mutation), so FreeIPA's DNS genuinely isn't running yet and the read-only plan-phase `dig` query gets no answer — expected on any fresh clean-room topology, not evidence of a broken authority. | Stopped and authorized live. Fixed by adding `and not ansible_check_mode` to the gate. Regression test added. Re-ran the preview: passed cleanly through the DNS gate section for both hosts. |
| 2 | Site-wide preview/apply fatal on `nexus`'s `freeipa-nfs-server`: "Gate: required roster and stage authorization", on a roster `pilot roster lint` reports clean | The roster schema-version gate (`schema_version \| int in [1, 2]`) never picked up `schema_version: 3` becoming the current default — `pilot edit`'s own NFS-server bootstrap writes a brand-new roster as `schema_version: 3`, so this gate rejected every freshly-created roster, while the sibling gate in `freeipa-identity-apply.yml` had already been correctly updated to include `3`. | Stopped and authorized live. Fixed by matching the sibling gate exactly (`[1, 2, 3]`). Regression test added, also asserting both gates' lists stay identical. |
| **3** | **`✅ 套用完成`, `failed=0` on all hosts** | Clean pass after both fixes. See below. | — |

## Attempt 3 — the clean pass

Topology came up clean on the first `topology up` — no wire-race retry needed.

```
client-vm      : ok=184  changed=60   unreachable=0    failed=0    skipped=60   rescued=0    ignored=1
freeipa-server : ok=133  changed=54   unreachable=0    failed=0    skipped=32   rescued=0    ignored=0
localhost      : ok=1    changed=0    unreachable=0    failed=0    skipped=0    rescued=0    ignored=0
nexus          : ok=346  changed=122  unreachable=0    failed=0    skipped=113  rescued=0    ignored=0
```

## Fix verification

**Fix #1 — DNS preflight check-mode exemption:** re-ran the exact same `--check --diff` preview
after the fix; it passed cleanly through the previously-failing gate on both `client-vm` and
`nexus`, reaching `✅ 預覽完成，沒有錯誤。`.

**Fix #2 — NFS-server roster schema gate:** re-ran the preview after both fixes; it passed the
NFS-server gate and continued through the rest of the run to the same clean result.

## Day-2 reconcilers and single-component deploys

- `freeipa-identity` reconcile: `ok=103 changed=26 failed=0`, `✅ 套用完成`.
- `freeipa-dns` reconcile: `ok=31 changed=2 failed=0`; both A records (`wazuh`/`s3` → nexus)
  verified live via `dig`.
- `freeipa-dns-client`: `client-vm`/`nexus ok=20 changed=7`, `freeipa-server ok=18 changed=3`, all
  `failed=0`.
- `freeipa-ca-trust`: all three hosts `ok=11 changed=2 failed=0`.
- `reverse-proxy`: `nexus ok=9 changed=5 failed=0`.
- `internal-endpoint` reconcile: `freeipa-server ok=82 changed=11 failed=0`; `client-vm`/`nexus
  ok=25 changed=0`.
- §3.9 suggester: CLI spot-check only this round (no new endpoint published) — correctly skipped
  the already-published `grafana.it.pilot.internal` and proposed a new candidate,
  `wazuh-dashboard.it.pilot.internal`, for the undeclared `wazuh-manager` dashboard service.

## Idempotency reruns

- `freeipa-dns` + `internal-endpoint` (combined multi-select rerun): `changed=0` on every targeted
  host, for both preview and real apply.
- `freeipa-dns-client`, `freeipa-ca-trust`, `reverse-proxy` (individual reruns): all `changed=0`.
- A full site-wide idempotency rerun was attempted and blocked at the delivery-transaction
  preflight gate — `wazuh-manager` on `nexus` reported only 43 GiB free against its declared 50 GiB
  minimum, after the real deploy's own data (Wazuh indexer, Prometheus/Loki TSDB, Docker images)
  had accumulated on disk. Environment/topology-sizing gap (the topology's 80 GiB `nexus` disk was
  sized against the bare minimum, not against a populated stack), not a code defect. Not fixed —
  recommend bumping `nexus`'s topology disk allocation in a future round.

## §4.1–§4.5 verification matrix

**§4.1 HBAC/sudo** — `scripts/minimal-poc-section4-spotcheck.sh` reached **8/8** after fixing a
genuine roster-authoring gap found mid-round (the HBAC rule was missing `sudo`/`sudo-i` services,
causing `sudo -n` to fail even though `hbactest` allowed it). Real-credentialed HBAC denial
confirmed via `journalctl -u ssh` on `nexus`: `pam_sss(sshd:auth): authentication success` →
`pam_sss(sshd:account): Access denied for user bob: 6 (Permission denied)`.

**§4.2 metrics/logs** — Thanos `up{site="round30-nexus"} == 1`. Full log chain: per-host
`audit.log`/`auth.log` landing files present and current; per-host marker injection returned only
for its own host label; coverage query non-zero for all three (`client-vm=201, ipa1=679,
nexus=3868`); Wazuh-alert host attribution confirmed real (FQDN format).

**§4.3 backup/FIM** — `restic-backup.timer` active+enabled on all 3 hosts. A real triggered backup
on each host produced 3 fresh snapshots, one per host by real FQDN (`ipa1.ipa.pilot.internal`,
`nexus.ipa.pilot.internal`, `client-vm.ipa.pilot.internal`). A real-time Wazuh `whodata` alert
(`added` event, `syscheck_new_entry` decoder) was captured end-to-end through Loki for a
live-injected `/etc` file on `client-vm`.

**§4.4 identity reconciler cycle** —
1. Removed alice's HBAC/role-group membership. Reconcile: `changed=1`. Live-discovered the
   HBAC-services gap (see §4.1 above); fixed via `pilot edit` and reconciled again (`changed=2`).
2. This second reconcile also silently reset alice's/bob's password back to the roster's stale
   `initial` value — **not a new bug**, the existing runbook §6.1 row on this mechanism already
   documents it (the roster still carried `password.force_change: true` from onboarding).
3. Flipped `password.force_change: false` for both users; re-personalized alice via a scripted
   forced-change `kinit`; reconciled again — `changed=0`, confirming the fix held.
4. Restored alice's membership and the HBAC/sudo fix in the same roster edit. Reconcile:
   `changed=3`.
5. Idempotency rerun: `changed=0` on the first try — cleaner than round 29's equivalent cycle, which
   needed one extra pass because a control-user's password had been reset without being
   re-personalized.

**§4.5 CA trust/DNS/endpoint matrix** — C-ca-1 `trusted` on all three hosts; C-dns-1
`pilot-managed` on all three; C-dns-2 `grafana.it.pilot.internal` resolves to nexus's real IP
(`192.168.122.2`) from all three; C-endpoint-1 `curl` succeeds with **no** `-k`, and the served
certificate genuinely chains to FreeIPA's own CA (`issuer=... CN=Certificate Authority`, not
self-signed).

## Findings for §6 gotchas

Both fixes folded into §6.2 as new rows (not committed at time of writing, so shown without a
commit hash) — see there for the authoritative symptom/fix text. The stray-`group_vars`
environment-pollution row and a new nexus-disk-headroom row were also added/extended in §6.1.

## Findings reported, not fixed (out of this skill's read-only/authorization scope)

- `pilot edit`'s internal-endpoint suggester menu item (`pushInternalEndpointSuggestMenu` in
  `cmd/pilot/cmd/edit_tui_internal_endpoints.go`) always reports 0 `reverse-proxy` hosts, because it
  resolves inventory groups against `hosts.yml` via `ansible-inventory` instead of the generated
  `inventory.yml`. The standalone CLI form is the recommended workaround (§3.9).

## Documentation-only corrections folded into this round's runbook update

- The runbook's top-of-file status banner had never been updated past round 28, despite round 29
  already having a full §7 record — this predates round 30 and is not something this round caused,
  but is fixed as part of this round's own banner update.
- HBAC rule `subjects.groups` now accepts `team`/`role`/(legacy) `access` category groups directly,
  not `access-*` only — §2/§3.3/§4.4 updated.
- The canonical roster schema default is now `schema_version: 3`, not `2` — every generic reference
  updated (round-specific historical §7 records for earlier rounds left untouched, since those
  correctly describe what that round actually used).
- The roster manager's top menu has two new, unexercised items (`🏛️ Access governance`, `🔒 Identity
  hardening`) — noted as out of scope for this minimal PoC's baseline.
- The site-wide deploy's auto-detected `-e` prompt set is no longer a fixed count of seven — it
  shrinks as group_vars autofill persists across runs, and two new topology-independent
  `detection-engine` prompts now also fire.

## Not re-run this round

Nothing in the documented §0.5–§4.5 scope was skipped — this was a full run start to finish, plus
the idempotency-rerun sweep. `docs/verification/*.md` per-component spec checklists beyond what
§4's own matrix already covers were not separately re-run; that is the same boundary every prior
round drew.

## Teardown

All three VMs torn down cleanly via `pilot vm-target topology down` against the same topology spec
used to bring them up. The `pilot services` dev-lite host-level cache was left running (shared
across rounds, not part of the disposable candidate). The run's own workspace/casts/evidence were
left in place per §5's explicit "do not delete" rule, for the user's own review.

## Functional verdict

PASS for the complete §0.5–§4.5 scope, on a genuine fresh clean-room 3-node rebuild, after 2 real
regressions were found and fixed live with explicit authorization at each stop, plus a full
idempotency-rerun sweep (all `changed=0` except the pre-existing environment/disk-sizing gap on a
full site-wide rerun, not a code defect).
