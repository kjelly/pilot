# Alertmanager actionable Teams routing evidence — 2026-09-17

## Candidate identity

| Field | Value |
|---|---|
| Tested commit | `30d6b92c4a69a9af2e4f7b219d51e740c0a9e3f4` |
| Tested tree | `ce03c8b205d4d68e2e5b3c533a1bb84b8ad8ce93` |
| Checkout | clean detached worktree `/tmp/pilot-detection-30d6b92` |
| Raw verify artifact | `.verification/alertmanager-20260917-064600.{md,ndjson}` |
| Playbook SHA-256 | `115f16fa3bc374737f677868981e5aa038e2a3ce886ee33e6097d89aac20df66` |
| Teams proxy SHA-256 | `7550cb6f407e673060c464fe4b8c77e4f55e22ea5c5e8e5abd02bb10146effcf` |
| Verification spec SHA-256 | `be9f34bbf36cf3276a47988ba283b962d8d2bd785122ae6977d17343533a1aee` |

## Target and result

A disposable Ubuntu 24.04 VM, `detection-fixture-provider`
(`192.168.122.11`), was rendered into an inventory with the `alertmanager`
and `docker` groups. Docker was applied first. Teams mode used an inert
`https://example.invalid/...` URL; `/render` does not forward to that URL.

| Gate | Result |
|---|---|
| L1 syntax | PASS |
| L3 `--check --diff` | PASS — `changed=0 failed=0` |
| L4 Teams-mode apply | PASS — `ok=16 changed=6 failed=0 unreachable=0` |
| L5 verify | PASS — C1–C9, `pass=9 fail=0 skip=0` |
| L6 idempotency | PASS — `ok=16 changed=0 failed=0 unreachable=0` |

The live rendered configuration check returned `routing-contract: PASS` for:

- `group_by: [alertname, pilot_subject, signal_id]`;
- `repeat_interval: 12h`;
- Teams routes for `severity="critical"` and
  `action_required="true"`.

The running proxy's loopback `/render` endpoint returned
`adaptive-card-actionability: PASS`; the card contained the supplied reason,
recommended action, runbook URL, and duration. No Teams message was sent.

The first single-VM `vm-target test` attempt applied successfully but L5 could
not resolve the spec's declared `alertmanager` group and automatically rolled
back. The final evidence run used a rendered group-aligned topology inventory;
all six stages then passed. This preserves the failed attempt instead of
misreporting it as a product failure.

