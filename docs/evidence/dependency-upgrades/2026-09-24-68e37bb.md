# Dependency upgrade: Go 1.26.8, x/crypto, x/net — gateway Kerberos/SPNEGO path (2026-09-24)

PR: kjelly/pilot#4. The upgraded x/crypto and x/net reach pilot through gokrb5, which `internal/freeipaaccess` uses for keytab login and SPNEGO calls to the FreeIPA JSON-RPC API. This run exercises that path on real VMs with binaries built from the candidate.

| Item | Value |
|---|---|
| Tested candidate | `68e37bb` (tree `c9d92254eb0263aaabee64bc959bc666e6bb3818`), built and run from a clean detached worktree |
| Execution-affecting files (sha256 prefix) | `go.mod` `5f3b44d2369ee1d5`, `go.sum` `0ab40666b09d5d0f`, `.github/workflows/ci.yml` `71ba6480d11096fb` |
| Binaries (linux/amd64, from the candidate) | `pilot` `e880d32b0636afc7`, `pilot-access-gateway` `284fb965bcff2ca6`. Both report go1.26.8, x/crypto v0.57.0, x/net v0.59.0, gokrb5 v8.4.4 |
| Topology | A per-run copy of `docs/topologies/pilot-access-transport-topology.yaml` with the nodes renamed `dx-ipa`, `dx-gw`, `dx-target`, `dx-ws`, because another session was using the `tx-*` names. Created with `topology up` and removed with `topology down` after the run |
| Playbook | `playbooks/test/pilot-access-transport-e2e.yml` with `-e '{"gateway_scope_hosts":["dx-target.ipa.pilot.internal"]}'`, `pilot_binary_path`/`pilot_access_gateway_binary_path` pointing at the candidate build, and a vault file supplying `ipa_admin_password` |
| Run | 2026-09-24, 03:07–03:26 UTC |
| Verdict | **PASS** |

## Topology test on never-applied VMs

| Step | Result |
|---|---|
| L1 syntax | passed |
| L3 `--check --diff` | `dx-gw changed=4`, `dx-ipa changed=3`, `dx-target changed=5`, `dx-ws changed=2`, `failed=0` everywhere |
| Cluster snapshot | tag `pre-test-1790219329` |
| L4 apply | `dx-gw ok=241 changed=50`, `dx-ipa ok=60 changed=20`, `dx-target ok=214 changed=26`, `dx-ws ok=5 changed=3`, `failed=0` |
| L5 `pilot verify` | gateway spec **PASS 16/16** (AG01–AG04, AG06, AG09, AG12, AG19, AG30, AG32–AG34, AG41–AG44); target policy spec **PASS 5/5** (TP01–TP05) |
| L6 idempotency | `changed=0` on all four hosts |

Three rows go through `internal/freeipaaccess`: AG03 checks the service keytab, and AG06 and AG12 read `/v1/health`, which pings FreeIPA JSON-RPC and looks up the target hostgroup.

## Direct probes of the Kerberos path on `dx-gw`

| Probe | Result |
|---|---|
| Deployed gateway binary | sha256 `284fb965bcff2ca6`, the candidate build |
| `/v1/health` as root | `status: ok`, `freeipa: reachable`, `target_scope: ok`, `credential: ok` |
| `/v1/identity` as `transportuser` (member of `role-pilot-portal-user`) | HTTP 200 with the user's uid and the gateway identity |
| `/v1/access` as `transportuser` | HTTP 200. One host, `dx-target.ipa.pilot.internal`: SSH allowed by HBAC rule `allow_all`, sudo scope `none` |
| `/v1/access/dx-target.ipa.pilot.internal` as `transportuser` | HTTP 200, same decision |
| Restart the gateway socket and service, then `/v1/health` and `/v1/access` | HTTP 200 for both. The first request after the restart logged in again from the keytab |
| Gateway journal before the outage test | no `error`, `krb`, `kerberos`, `spnego` or `keytab` lines |
| Stop `httpd` on `dx-ipa` | `/v1/health` → HTTP 503 (`status: degraded`, `freeipa: unreachable`, `credential: unknown`); `/v1/access` → HTTP 503 `access service unavailable`. Journal: `resolve access failed … connect: connection refused` |
| Start `httpd` again | both endpoints back to HTTP 200 within 5 s, without restarting the gateway |

The outage step shows the reachable result above comes from live FreeIPA calls, not a cache, and that the gateway fails closed.

## Deviations

- The topology was a renamed copy of the committed one. The playbook, specs and variable contract were unchanged.
- Before trusting the probes, `ipa1.ipa.pilot.internal` on `dx-gw` was confirmed to resolve to this run's `dx-ipa`.

## Redaction

Vault values and the fixture user's credentials are not recorded. Raw playbook output, verify NDJSON and probe logs stayed in the scratch area and are not committed.
