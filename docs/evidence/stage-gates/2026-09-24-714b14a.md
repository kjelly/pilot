# Prod attestation gate: stage=prod can pass (2026-09-24)

Covers candidate `714b14a` on top of `main` `548f35b`. Ten "Gate: prod requires recent staging attestation" gates in nine apply playbooks had `that: [stage != 'prod', (staging_attested_within_hours | int) <= 168]` with `when: stage == 'prod'`. Every `that:` item must hold and the gate only runs when stage is prod, so every stage=prod run failed there, whatever the attestation age. The candidate removes the `stage != 'prod'` item (`patch_stage` in os-patch-sla), matching the other 28 playbooks. No spec row changes.

| Item | Value |
|---|---|
| Tested candidate | `714b14a` (tree `4d892a0e1c642150f710f9feb794a244153c763c`), run from a clean detached worktree |
| Baseline | `main` `548f35b`, from its own clean worktree, same VM and inputs |
| Execution-affecting files (sha256, first 16 hex) | `playbooks/apply/`: `core-infra-provider-apply.yml 0cdcef25fcedcb76`, `docker-apply.yml 99749b5f876a919f`, `freeipa-ca-trust-apply.yml 971ff1cbb01157c6`, `internal-endpoint-apply.yml a9187dcf8b805034`, `keycloak-apply.yml a789672290f8788c`, `keycloak-db-apply.yml 341f39c72ed3773b`, `os-patch-sla-apply.yml 79e251d6b6be2121`, `reverse-proxy-apply.yml 6e02c5f5fb8c0b10`, `seaweedfs-s3-apply.yml a5dcb6ed8fce119a`; test: `internal/spec/assert_negates_when_regression_test.go d57c28ef9fa96421` |
| Target | one disposable `pilot vm-target`, `pa-prod`, Ubuntu 24.04, in the `prod` inventory group only (so the §4.3 group cross-check sees a real prod host). Controller ansible-core 2.19.2 |
| Inputs | the values `pilot deploy` passes when prod is chosen: `-e stage=prod` (`patch_stage=prod` for os-patch-sla), `-e confirm_prod=true`, `-e staging_attested_within_hours=<24 or 200>`; plus `-e target_group=pa-prod` (and `infra_role=dns` for core-infra-provider). No secrets beyond `~/.vault/main.yaml` |
| Go tests at candidate | `go test -race -count=1 ./...`: 52 packages ok. `TestRegression_AssertNeverNegatesItsOwnWhen` reports exactly these ten gates on `548f35b` and passes on the candidate. `pilot`'s spec/tag tests, `--syntax-check` of the nine playbooks and the duplicate-key check pass |
| Verdict | **PASS** |
| Run | 2026-09-24/25, operator: coding agent on the maintainer's workstation |

## Gate matrix (`--check`, so nothing past the gates is applied)

The gates are `assert` tasks, which behave the same in check mode. Each cell is the outcome of that playbook's attestation gate. The confirm gate passed in every run, and the group cross-check passed in every playbook that has one (reverse-proxy, freeipa-ca-trust and internal-endpoint have none).

| Playbook | Candidate, 24 h | Candidate, 200 h | `548f35b`, 24 h |
|---|---|---|---|
| core-infra-provider | pass | blocked | blocked |
| docker | pass | blocked | blocked |
| freeipa-ca-trust | pass | blocked | blocked |
| internal-endpoint, baseline play | pass | blocked | blocked |
| internal-endpoint, reconcile play | pass | blocked | blocked |
| keycloak | pass | blocked | blocked |
| keycloak-db | pass | blocked | blocked |
| os-patch-sla | pass | blocked | blocked |
| reverse-proxy | pass | blocked | blocked |
| seaweedfs-s3 | pass | blocked | blocked |

"blocked" is the gate's own message: "prod gate: staging attestation missing or older than 168h." (os-patch-sla: "prod gate: no staging attestation within 168h; …").

- On the candidate at 24 h, freeipa-ca-trust and internal-endpoint then stopped at their next gate, "a freeipa-server host is present in inventory", because the VM is not in that group.
- internal-endpoint's reconcile play only runs if the baseline play leaves the host standing, so it was run separately: `--tags always` with a second `-i` file that adds `pa-prod` to `freeipa-server`, and `internal_endpoint_baseline_target_group` pointed at an empty group so only the reconcile play ran. At 24 h on the candidate it then stopped at the next gate, which requires `-e internal_endpoint_manifest_file` (none was given).

## Real stage=prod apply

`docker-apply.yml` on `pa-prod` with `stage=prod`, `confirm_prod=true`, `staging_attested_within_hours=24`, not in check mode: rc=0, confirm, cross-check and attestation gates `ok`, `changed=2` (the Docker package install and the docker group membership), Docker 29.1.3 `active`. Re-run: rc=0, `changed=0`. On `548f35b` the same inputs stop at the attestation gate (check-mode row above).

## Redaction

`~/.vault/main.yaml` was passed as in other runs; no secret value appears here. The topology file, the extra inventory file and the matrix driver were per-run files outside the repository.
