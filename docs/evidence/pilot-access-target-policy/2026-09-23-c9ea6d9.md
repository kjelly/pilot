# Pilot Access target policy — Phase 4 vm-target evidence (2026-09-23)

Spec: `docs/superpowers/specs/2026-09-23-pilot-access-gateway-captive-ssh-transport-spec.md` §13, §14.4–§14.5, §16 Phase 4; verification spec `docs/verification/pilot-access-target-policy.md`.

| Item | Value |
|---|---|
| Tested candidate | `c9ea6d9` (tree `d24902d9374ca74cf3ff390e796139eaa6db162e`), built and run from a clean detached worktree |
| Topology | `docs/topologies/pilot-access-transport-topology.yaml`. The target is `tx-target` (Ubuntu 24.04, OpenSSH 9.6p1); the gateway address in the policy is `192.168.122.4` (`tx-gw`) |
| Go tests at candidate | `internal/spec`, `internal/contract`, `internal/inventory`, `internal/gatewayapi` ok; `cmd/pilot/cmd` bookkeeping subset (`TagAlignment\|Contract\|Spec\|Catalog\|Site\|GroupVars\|Inventory\|Deploy\|Apt`) ok |
| Verdict | **PASS** |

## Topology test (wrapper now includes the target policy)

| Step | Result |
|---|---|
| L1 syntax / L3 `--check --diff` | passed |
| L4 apply | all hosts `failed=0` (`tx-gw changed=3`, `tx-target changed=0`: a dev iteration had already applied the policy, see below) |
| L5 `pilot verify` | gateway spec **PASS 16/16**; target policy spec **PASS 5/5** (TP01–TP05) |
| L6 idempotency | `changed=0` on all hosts |

A pre-commit dev iteration from the working tree had already run the fresh-host path on `tx-target`: `--check --diff` passed (`changed=1`), the first apply gave `changed=4` (drop-in, reload, hostgroup-add, add-member), and the second apply gave `changed=0`. The Phase 5 `--ephemeral` run repeats check-mode on genuinely fresh VMs.

## Profiles, absent, idempotency (TP06, TP10, TP11)

| Run | rc | Recap | Changed tasks |
|---|---|---|---|
| strict → `remote-dev` | 0 | `changed=3` | snapshot previous drop-in; install drop-in; reload sshd |
| `remote-dev` again | 0 | `changed=0` | — |
| back to strict | 0 | `changed=3` | same three |
| strict again | 0 | `changed=0` | — |
| `pilot_access_target_policy_state=absent` | 0 | `changed=4` | remove from `pilot-transport-ready` → remove drop-in → remove snapshot → reload (task order 29 → 31: hostgroup first) |
| `absent` again | 0 | `changed=0` | — |
| restore strict | 0 | `changed=3` | install drop-in; reload; add to `pilot-transport-ready` (the policy block runs before the hostgroup join) |

- After each profile change, `pilot verify` on the target spec gave **PASS 5/5**, and the profile marker read `remote-dev` / `strict` accordingly.
- Hostgroup membership (TP06), checked through the gateway's own service principal: `MEMBER` after strict/remote-dev, `NOT-MEMBER` after absent, `MEMBER` after the restore.
- After absent (TP10): `sshd_config.d` held only `04-ipa.conf 05-freeipa-client-password-auth.conf 50-cloud-init.conf 60-cloudimg-settings.conf`, `sshd -t` passed, and the TP05 comparison was still identical.

## Invalid inputs (TP12)

The `*.conf` checksum was `93182cc3d08dd3c0` before, after, and between every run. The policy was still in force afterwards: **PASS 5/5**, `MEMBER`.

| Input | rc | Where it stopped |
|---|---|---|
| `pilot_access_target_forwarding_profile=open` | 2 | pre_tasks `Gate: policy inputs are valid` |
| `[]` | 2 | pre_tasks `Gate: gateway addresses are single IP addresses` |
| `["*"]` | 2 | same gate |
| `["0.0.0.0"]` | 2 | same gate |
| `["10.0.0.0/24"]` | 2 | same gate |
| `["192.0.2.1"]` (the TP05 TEST-NET address) | 2 | same gate |
| `["1::2::3"]` | 2 | passed the character check, `sshd -t -f`, and `sshd -t`, then failed the effective-value check (`sshd -T -C addr=1::2::3`). The `rescue` restored the previous drop-in and reloaded ("pilot-access-target-policy was NOT applied … previous sshd drop-in state was restored") |

The malformed-IPv6 case takes a different path from the one rev 3 of the spec predicted (`validate`), but the outcome is the same. The TP12 wording in both the spec and the verification doc now describes the observed path.
