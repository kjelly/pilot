# pilot-edit-mcp delivery evidence — 2026-08-04

- Candidate: working tree on top of commit `4cd9adbe81745370bad7f5c9eb14254858acdf78`
  (`cebec91` "trec: clarify recording lifecycle — save then close in two steps"),
  with these uncommitted changes on top (Phases 1-6 increment 1 of
  `docs/superpowers/specs/2026-08-04-pilot-edit-mcp-semantic-tui-design.md`, plus this
  session's "fix B and C" pass):
  - `AGENTS.md`, `README.md`, `TESTING.md` — doc updates (playbook-count drift fix,
    `pilot mcp serve` command-map/testing docs).
  - `cmd/pilot/cmd/contract_test.go`, `cmd/pilot/cmd/deploy_catalog.go`,
    `cmd/pilot/cmd/deploy_test.go`, `cmd/pilot/cmd/deploy_topology_graph_test.go`,
    `cmd/pilot/cmd/edit_tui_flows_test.go`, `cmd/pilot/cmd/mcp.go`,
    `internal/contract/contract_test.go`, `internal/contract/fixture_schema_test.go`,
    `internal/inventory/inventory_test.go` — the 4 pre-existing test-failure fixes plus
    their cascading golden-count updates (missing `freeipa-realm-replacement` contract,
    stale vault-section/roster/vault-masking test assumptions).
  - `contracts/freeipa-realm-replacement.yaml` (new) — the contract that was missing for
    an already-existing apply playbook.
  - The MCP server itself (`cmd/pilot/cmd/mcp.go`, `mcp_edit_tools.go`, `mcp_test.go`,
    `mcp_edit_tools_test.go`, `edit_automation*.go`, `edit_actions_registry.go`,
    `edit_tui_roster.go`, `edit_automation_driver_roster.go`, and the earlier
    Phase 1-5 files) predates this evidence-gathering pass; this document is the first
    full evidence sweep across all of it, gathered once the tree reached a fully green
    baseline.
- Operator: pilot (agent session), driving a real compiled binary through a real MCP
  client (`github.com/modelcontextprotocol/go-sdk/mcp`, the same module the server
  itself uses) and through `go test`'s own real-subprocess integration suite. No mock
  MCP tool handler was used as evidence anywhere in this document.
- Toolchain: `go version go1.26.4 linux/amd64`.

## Build

```
$ go build ./...
```

Result: clean, exit 0, no output.

## Focused unit test result

Real MCP-client integration tests (`cmd/pilot/cmd/mcp_test.go`) — each spawns the
compiled `pilot` binary as a subprocess via `mcp.CommandTransport` and drives it with a
real `mcp.NewClient` session, not an in-process mock handler:

```
$ go test -v -run TestMCPServe_Integration -count=1 ./cmd/pilot/cmd/
=== RUN   TestMCPServe_Integration_CapabilitiesInspectPlan
--- PASS: TestMCPServe_Integration_CapabilitiesInspectPlan (0.60s)
=== RUN   TestMCPServe_Integration_VaultPlanApplyRoundTrip
--- PASS: TestMCPServe_Integration_VaultPlanApplyRoundTrip (0.07s)
=== RUN   TestMCPServe_Integration_RosterPlanApplyRoundTrip
--- PASS: TestMCPServe_Integration_RosterPlanApplyRoundTrip (0.07s)
PASS
ok  	github.com/kjelly/pilot/cmd/pilot/cmd	0.759s
```

## Full test result

```
$ go test ./... -count=1
```

Result: **1303 passed, 0 failed, 21 packages** — every package `ok`, no `FAIL` lines.
(This is the first time this repo's full suite has been fully green; previously 4
pre-existing failures were tolerated as out-of-scope across Phases 1-6 — see this
session's own fixes above.)

No side effect on `playbooks/apply/*.yml` (`git status --short playbooks/apply/` empty
before and after).

## Race test result

```
$ go test -race ./... -count=1
```

Result: **1303 passed, 0 failed, 21 packages** — identical clean result under the race
detector.

## Interactive `pilot edit` regression result

```
$ CI=1 go test -run TestEditRouter -count=1 -v ./cmd/pilot/cmd/
```

Result: **44 passed, 0 failed** (router unit tests + Bubble Tea `teatest` flow tests,
covering role-preset/host/vault flows, ESC/dirty-discard gates, and router-level
transition/banner/cancel semantics). Confirms the semantic-action/MCP work in this
delivery did not regress the ordinary interactive wizard.

## Manual real-MCP-client session (plan + apply, non-mock)

A standalone Go program (`mcp.NewClient` + `mcp.CommandTransport`, deleted after use —
never committed) spawned the freshly built binary as `pilot mcp serve --dir <tmp>
--audit-dir <tmp>/.pilot-audit --allow-write` and drove it exactly as an external coding
agent would: `ListTools` → `pilot_edit_inspect` → `pilot_edit_plan` →
`pilot_edit_apply` → `pilot_edit_inspect` again.

**Tools listed:** `pilot_edit_apply`, `pilot_edit_capabilities`, `pilot_edit_inspect`,
`pilot_edit_plan` (all four, since `--allow-write` was set).

**Scenario:** `create_host evidence-01` → `set_host_field ansible_host=10.9.9.9` →
`save_hosts`.

### Plan call (workspace hash before/after)

- `plan_id = d588fe928a4b09adc357d2079ecd09d7`
- `base_revision` (before) = `sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
  (empty workspace)
- `affected_files = [hosts.yml]`, `redacted_diff = false`
- Real workspace **unchanged** by the plan call: `hosts.yml` did not exist on disk after
  planning, and re-hashing the real workspace directory after the call reproduced the
  same `sha256:e3b0...` as before — the plan ran only against a temporary copy.
- `diff.patch` content:
  ```diff
  --- a/hosts.yml
  +++ b/hosts.yml
  @@ -0,0 +1,4 @@
  +hosts:
  +  evidence-01:
  +    ansible_host: "10.9.9.9"
  +    roles: []
  ```
- Plan audit directory:
  `<tmp>/.pilot-audit/20260804T093226Z-d588fe928a4b09adc357d2079ecd09d7-plan/`
  containing `diff.patch, metadata.json, scenario.redacted.json, session.cast,
  trace.jsonl, validation.json`.

### Apply call (workspace hash before/after)

- `plan_id = d588fe928a4b09adc357d2079ecd09d7`, `expected_revision` = the same
  `sha256:e3b0...` captured above.
- `result = applied`, `rolled_back = false`
- `revision_before = sha256:e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`
- `revision_after  = sha256:76ebd8f974abb892fc0f5fd03c8963fea7a18fc4a416a13e1fe2eb6fe35f34c4`
  (hash changed, confirming a real mutation)
- Re-running `pilot_edit_inspect` on the same live server session afterward reported
  `workspace_revision = sha256:76ebd8...` (matches `revision_after`) and listed
  `evidence-01` with `ansible_host: 10.9.9.9` under `hosts`.
- The real `hosts.yml` on disk after apply:
  ```yaml
  hosts:
    evidence-01:
      ansible_host: "10.9.9.9"
      roles: []
  ```
- Apply audit directory:
  `<tmp>/.pilot-audit/20260804T093226Z-f40d59ac9e30d663ed0426019a48149b-apply/`
  containing `diff.patch, managed-files-after.json, managed-files-before.json,
  metadata.json, result.json, scenario.redacted.json, session.cast, trace.jsonl,
  validation.json`.
- A second `pilot_edit_apply` against the same now-stale `plan_id`/`expected_revision`
  (exercised separately in `TestMCPServe_Integration_CapabilitiesInspectPlan`, see
  above) is rejected rather than silently reapplied.

## Recording check result

`session.cast` from the apply call above was read line-by-line and each non-empty line
was parsed as JSON independently (asciicast v2 = one JSON header line followed by JSON
event-array lines): **29/29 lines parsed successfully**, zero malformed lines. This
confirms the audit recording is a structurally valid asciicast v2 file, not just a file
that happens to exist.

## Secret sentinel scan result

A second scenario (`add_vault_key file=main.yaml key=manual_test_secret
value_env=PILOT_MANUAL_EVIDENCE_SENTINEL` → `save_vault`) was planned and applied
through the same live MCP session, with the sentinel value
`MANUAL-EVIDENCE-SENTINEL-9f3a1c` set only on the **subprocess's** environment (matching
how a real MCP client would supply a secret — never as a literal `value` over the wire).

- Every file under both the plan's and the apply's audit directories (`diff.patch`,
  `metadata.json`, `scenario.redacted.json`, `session.cast`, `trace.jsonl`,
  `validation.json`, `managed-files-before.json`, `managed-files-after.json`,
  `result.json`) was scanned for the literal sentinel string:
  **sentinel found in 0 files** (`audit artifacts leaked sentinel = false`).
- The real `.vault/main.yaml` on disk **does** contain the sentinel
  (`real .vault/main.yaml contains sentinel = true`), confirming the value really was
  written to the workspace — the scan above is a redaction check, not a "nothing
  happened" false negative.
- This matches `TestMCPServe_Integration_VaultPlanApplyRoundTrip`'s own
  `assertSentinelAbsent` assertions (also PASS, see Focused unit test result above),
  which walk the same audit directories plus the raw `CallToolResult` JSON for both the
  plan and apply calls.

## Raw artifacts

- `/tmp/claude-1000/-home-kjelly-github-pilot/d68d687c-852a-4ba6-9ffd-658454c6c7df/scratchpad/mcp_manual_evidence.log` — full stdout of the manual real-client
  session above (not committed; local to this session's scratchpad).
- `/tmp/claude-1000/-home-kjelly-github-pilot/d68d687c-852a-4ba6-9ffd-658454c6c7df/scratchpad/mcp_integration_tests_verbose.log`,
  `full_test_run.log`, `race_test_run.log`, `edit_regression.log` — full `go test`
  output backing the four test-result sections above (not committed; local to this
  session's scratchpad).
- The manual evidence client program itself was a throwaway (`mcpevidence_check/main.go`
  at repo root during the run) and was deleted after use — never committed, per this
  document's own record rather than git history.
