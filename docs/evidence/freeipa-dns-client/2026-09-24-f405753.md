# freeipa-dns-client: no SIGPIPE in the resolver snapshot (2026-09-24)

Covers candidate `f405753` on top of `main` `19b54de`. The resolver snapshot ran `nmcli -t -f NAME,DEVICE connection show --active | head -n1` under `set -euo pipefail`; when `head` exited before `nmcli` finished writing, the pipeline exited 141 (SIGPIPE) and `set -e` stopped the snapshot. `TestFreeipaDNSClientRollback_ELRestoresNetworkManagerSettings`, which runs the real script against a fake `nmcli`, failed that way on main's CI for `fa29a2d` and `19b54de`. The checklist and Expected are unchanged.

| Item | Value |
|---|---|
| Tested candidate | `f405753` (tree `fa6ddd09a6f294f74648718a3888fa824e82d55f`), run from a clean detached worktree |
| Superseded candidate | `9034c72`: its first real run failed at L3 on the EL client with "failed at splitting arguments, either an unbalanced jinja2 block or quotes": an apostrophe in the new comment. Fixed in `f405753`; none of its results are used |
| Execution-affecting files (sha256, first 16 hex) | `playbooks/apply/tasks/freeipa-dns-client-resolver.yml da83cea771ad9056`; tests: `internal/spec/shell_pipefail_regression_test.go dc292aa5de1e5692`, `internal/spec/freeform_args_regression_test.go cc5f72e85f69e07c` |
| Topology | two disposable `pilot vm-target`s (`services: local`): `g11-ipa` AlmaLinux 9, `freeipa-server`; `g11-el` AlmaLinux 9, `freeipa-client` + `dns-clients`. Controller ansible-core 2.19.2 |
| Scope | Only the EL (NetworkManager) branch of the snapshot changed, so only an EL client was run. The Debian/Ubuntu path is unchanged and was last run at `62d8069` (`2026-09-24-62d8069.md`) |
| Go tests at candidate | `TestFreeipaDNSClientRollback_ELRestoresNetworkManagerSettings`: 4 failures in 300 runs before the fix (`snapshot rc=141`), 0 in 2 × 1000 + 500 runs after. New lints fail on the unfixed lines. golangci-lint v2.12.1 on `internal/spec`: 0 issues |
| Verdict | **PASS** |
| Run | 2026-09-24, ~07:53–08:20 UTC, operator: coding agent on the maintainer's workstation |

## Runs

1. `pilot vm-target topology test --playbook playbooks/site.yml --verify docs/verification/freeipa-server.md=freeipa-server`: rc=0. L3 check mode on the fresh topology passed; L4 `g11-ipa changed=16`, `g11-el changed=18`; freeipa-server verify **PASS 20/20**; L6 `changed=0`.
2. `pilot vm-target topology test --playbook playbooks/apply/freeipa-dns-client-apply.yml --verify docs/verification/freeipa-dns-client.md=dns-clients -- -e target_group=dns-clients`: rc=0. L3 passed; L4 `g11-el changed=2`, the snapshot task `ok`; verify **PASS 6/6**; L6 `changed=0`.
3. Rescue, re-run with `-e '{"freeipa_dns_client_servers": ["192.0.2.53"]}'` (TEST-NET-1, never answers): rc=2, `rescued=1`, the intended outcome. The rescue printed "Rollback: /etc/resolv.conf restored; connection 'System eth0' DNS settings restored; device eth0 reapplied." Before and after: `ipv4.dns=192.168.122.5`, `ipv4.dns-search=ipa.pilot.internal`, `ipv4.ignore-auto-dns=yes`, device `IP4.DNS 192.168.122.5`, `getent hosts ipa1.ipa.pilot.internal` rc=0.

## Lints added

- `TestRegression_PipefailShellTasksHaveNoEarlyExitReader`: no shell task that sets pipefail pipes into `head`, `grep -q`/`-m`, `sed …q` or `awk …exit`. The sweep found no other such pipe in `playbooks/**`; the four in `scripts/*.sh` run on the remote side or sit in `$(…)` without `set -e`.
- `TestRegression_FreeFormCommandsSplitInAnsible`: every free-form shell/command/raw/script string passes Ansible's `split_args` quote and Jinja rules, comments included. Its unit cases are verdicts from ansible-core 2.19.2's `split_args`; running the real `split_args` over all 281 free-form strings found only the `9034c72` comment, and the lint agrees.

## Found, not fixed here

- freeipa-server C16 (`test -s` on the 389-ds audit log) failed 19/20 on a first attempt whose topology had no enrolled client. With `g11-el` enrolled, C16 passed.
  - *Corrected 2026-09-24.* This record first said nothing had written to the directory since audit logging was enabled. That was a guess, and it was wrong: `freeipa-server-apply.yml` writes a dummy `description` for C16 when the audit file is empty.
  - The cause is 389-ds audit log buffering. Reproduced on the same topology shape: main `fa974c7`, AlmaLinux 9 server `g12-ipa` plus an unenrolled AlmaLinux 9 `dns-clients` host; 389-ds-base 2.8.0-10.el9_8, where `nsslapd-auditlog-logbuffering` is `on`. The playbook does not set it.
  - Timeline (UTC): audit enabled 08:43:49; the dummy write logged at 08:43:50 with `result: 0`; `site.yml` done 08:43:52; verify at 08:44:08 found the file empty (C16 fail, 19/20); the file was first written at 08:44:13.
  - A write probe, after each `ldapmodify` polling the file size every second: with buffering `on`, 8 s and 29 s until the file grew; with it `off`, 0 s twice. The setting was then restored to `on`.
  - That fits C16 passing when a client enrolled: the client plays run after the server play, which leaves time for a flush before verify. With no client, verify ran 16 s after the apply ended, inside the flush window.
  - Not fixed. Two ways to fix it: the playbook could wait until the audit file is non-empty after the dummy write, or turn buffering off (with it on, the newest audit entries sat in memory for 8–29 s here before they reached the disk).

## Test-environment notes (not product changes)

- The first attempt's automatic cluster rollback restored the snapshot-time guest clocks; they were set from the controller with `date -u -s` before the formal runs.

## Redaction

`~/.vault/main.yaml` supplied `ipa_admin_password`; no secret value appears here. The topology file was a per-run file outside the repository.
